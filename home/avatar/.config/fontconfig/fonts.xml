<?xml version="1.0"?>
<!DOCTYPE fontconfig SYSTEM "fonts.dtd">
<fontconfig>

<!-- 原有字體映射配置（保持不變） -->
<match target="pattern">
  <test name="family">
    <string>system-ui</string>
  </test>
  <edit name="family" mode="prepend" binding="strong">
    <string>sans-serif</string>
  </edit>
</match>

<match target="pattern">
  <test name="family">
    <string>sans-serif</string>
  </test>
  <edit name="family" mode="prepend" binding="strong">
    <string>微信公众号：数码迷（ID：wanshuma）</string>
  </edit>
</match>

<match target="pattern">
  <test name="family">
    <string>serif</string>
  </test>
  <edit name="family" mode="prepend" binding="strong">
    <string>FOT-TsukuBOldMin Pr6</string>
  </edit>
</match>

<match target="pattern">
  <test name="family">
    <string>monospace</string>
  </test>
  <edit name="family" mode="prepend" binding="strong">
    <string>JetBrains Maple Mono</string>
  </edit>
</match>

<!-- 新增：明確指定 RGB 子像素順序（解決模糊） -->
<match target="font">
  <edit name="rgba" mode="assign">
    <const>rgb</const>
  </edit>
</match>

<!-- 新增：啟用 LCD filter + 輕量 hinting（Mint 效果）-->
<match target="font">
  <edit name="antialias" mode="assign"><bool>true</bool></edit>
  <edit name="hintstyle" mode="assign"><const>hintslight</const></edit>
  <edit name="lcdfilter" mode="assign"><const>lcddefault</const></edit>
</match>

<!-- 新增：排除位圖字體（避免模糊）-->
<selectfont>
  <rejectfont>
    <pattern>
      <patelt name="family"><string>Fixed</string></patelt>
    </pattern>
  </rejectfont>
</selectfont>

</fontconfig>
